import { FilterByBirthdayPipe } from './filter-by-birthday.pipe';

describe('FilterByBirthdayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBirthdayPipe();
    expect(pipe).toBeTruthy();
  });
});
